# EverythingMatrix

A lightweight browser-based matrix calculator (no framework).

## Run locally
Open `public/index.html` in your browser.

> Tip: If your browser blocks ES modules via file://, run a tiny server:
> - VS Code “Live Server” extension, or
> - `python -m http.server` from the repo root, then open `/public/`.
